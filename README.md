# Learn Laravel

This repository contains a simple bookmark app to learn Laravel.

## MemorableSites

The app is a simple bookmark management app, which allows its users to:

- add a new bookmark to a flat list of bookmarks
- tag bookmarks with one or more `key:value` pairs
- add a short description to each bookmark
- list all bookmarks available
- list all bookmarks with a specific `key:value` tag